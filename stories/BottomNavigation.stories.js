import { createBottomNavigation as createBottomNavigationFloating } from '../components/bottomNavigation/floating';

export default {
    title: 'Components/BottomNavigation',
    tags: ['autodocs'],
};

export const Floating = {
    render: () => createBottomNavigationFloating(),
}