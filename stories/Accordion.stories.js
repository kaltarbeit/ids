import { createAccordion } from '../components/accordion';

export default {
    title: 'Components/Accordion',
};

export const 콘텐츠형 = {
    render: () => createAccordion(),
    tags: ['autodocs'],
}

export const 리스트형 = {
    render: () => createAccordion(),
    tags: ['autodocs'],
}