import { createShareBottomSheet} from '../../patterns/share/custom';

export default {
  title: 'Patterns/Share',
  tags: ['autodocs'],
};

export const Custom = {
    render: () => createShareBottomSheet(),
}