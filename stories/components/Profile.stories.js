import { createProfile } from '../../components/profile/default';

export default {
    title: 'Components/Profile',
    tags: ['autodocs'],
};

export const Default = {
    render: () => createProfile(),
}